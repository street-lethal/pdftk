services:
  pdftk:
    build: .
    working_dir: /app
    environment:
      LANG: C.UTF-8
    user: "${USER_ID}:${GROUP_ID}"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
      - ./scripts_in_docker:/app/scripts
      - /etc/group:/etc/group:ro
